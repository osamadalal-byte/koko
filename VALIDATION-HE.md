# FORM 28 — תוצאות בדיקה ומצב פרסום

תאריך: 2026-09-16. גרסה: 1.0.1-rc.

הקוד הועלה בפועל ל־[osamadalal-byte/koko](https://github.com/osamadalal-byte/koko), בשורש הענף `feature/form28-release`, כולל `.github/`, ללא תיקיית מעטפת. [בקשת מיזוג #1](https://github.com/osamadalal-byte/koko/pull/1) פתוחה. המאגר הציבורי סופק בידי המשתמש והעלאה ציבורית אושרה במפורש. לא שונתה נראות ולא הופעל חיוב. **אין עדיין אתר HTTPS חי שנבדק.**

## תוצאות בפועל

[ריצת GitHub Actions שעברה](https://github.com/osamadalal-byte/koko/actions/runs/35104289642) בדקה את קוד האפליקציה ב־commit `ef97add870aba108035309a5e83719fe8edbd238`. הדוח השמור נמצא ב־`validation/github-2026-09-16/browser-results.json`. ריצות נוספות אחרי עדכוני התיעוד מוצגות ב־Actions.

| בדיקה | תוצאה |
|---|---|
| התקנת תלויות npm | עברה ב־GitHub Actions |
| התקנת Chromium ו־WebKit עם תלויות מערכת | עברה ב־GitHub Actions |
| npm test | עבר: 280 תרחישי תזמון ובדיקות התאמה, שמירה, ארכיון ומודל מטמון |
| npm run test:browser | עבר: כל ששת התרחישים ב־macOS, Chromium ו־WebKit בהדמיית iPhone 13 |
| npm run build | עבר |
| node tests/check-preview.cjs | עבר |
| npm run test:dist | עבר בשורש ובנתיב /koko/ |
| בדיקה חזותית | נבדקו צילומי WebKit; תוקן אייקון גדול שדחק את כפתורי הטיימר |
| מקורות וידאו | כל 14 דפי המקור עבור 18 תנועות נפתחו ב־HTTP 200; נצפה ניגון ב־7 דפים; אין אימות מלא לכל הגרסאות |
| אתר חי והתקנה באייפון | טרם נבדקו |

תרחישי הדפדפן כוללים מקור בשורש, `dist` בשורש ו־`dist` תחת `/koko/`, בכל אחד משני המנועים. עברו גלילה, רוחבי 320/375/390 פיקסלים, ניווט וכפתורים, הוראות לכל 18 התנועות, בדיקת מוכנות, טיימר בזמן אמת ועצירה, טעינה מחדש והמשך, שמירה ב־pagehide ופתיחה במצב מושהה, משוב, ייצוא קובץ גיבוי וייבואו, דחיית גיבוי פגום, manifest, אייקונים ותחום service worker.

## איך נבדקה עבודה ללא רשת

ב־Playwright 1.62 הופיעה בעיית `navigator.onLine`; נבחרה גרסה 1.61.1. ב־WebKit, גם על Linux וגם על macOS, `setOffline(true)` ואז טעינה מחדש החזירו שגיאה פנימית. בדיקת ביקורת עצמאית שחזרה את אותה שגיאה עם service worker זעיר, ללא קוד האפליקציה. לעומת זאת, גם דף הביקורת וגם FORM 28 נטענו כשהשרת עצמו נותק. הראיות נשמרו ב־`offline-diagnostic.json`, והבדיקה ניתנת לשחזור עם `node tests/offline-probe.cjs` אחרי בנייה.

הבדיקה הסופית, זהה בשני המנועים, מאמתת תחילה את אירוע הניתוק והחיווי. לאחר מכן שרת האפליקציה מפסיק לספק קבצים, נבדק שבקשה לא־שמורה נכשלת, והדפדפן נדרש לטעון מחדש מהמטמון ולהפעיל טיימר, הוראות ושמירת טיוטה. כל הדרישות האלה נשארו תנאי מעבר; לא הוסר תרחיש ולא סומנה תקלה כהצלחה. זו בדיקת דפדפן מול שרת מנותק, **לא מצב טיסה במכשיר פיזי**.

## תיקונים שנכללו

- מניעת ערבוב HTML חדש עם JavaScript ישן במטמון.
- ניקוי `dist` הנוצרת בכל בנייה וגרסת מטמון הנגזרת מתוכן הקבצים.
- חיווי חיבור שמזהה גם אתר לא נגיש כשהמכשיר עדיין מדווח שהוא מחובר לרשת.
- תיקון גודל אייקון הווידאו כדי שכפתורי הטיימר יישארו במסך האימון.
- תיקון בורר כפתור הפרופיל בבדיקת הדפדפן, בלי להסיר את בדיקת עריכת הפרופיל.
- התאמת ההסברים למתיחת חזה ושוק לגרסאות המצולמות ב־NHS; נשמרו חלופות קלות.
- קישורי גיבוי לדפי הספק והבהרה שסרטון הנשימה כולל מגיש ותרשימים.
- נשמרו מפתח `form28-v1`, מבנה הנתונים, מסלול האימון, ההתאמה, הטיימר, הגיבוי וארכיון המחזורים.

## וידאו — מגבלות האימות

הדוח `validation/github-2026-09-16/video-observations.json` מפרט כל תנועה. נצפו זמן ניגון מתקדם ותמונות מתוך סרטוני NHS, Hinge Health ו־Royal Marsden/Vimeo. תמונות בודדות אינן צפייה מלאה בסרטון. מקורות YouTube ביקשו אימות כניסה/בוט בסביבת GitHub, ולכן ניגונם לא אומת. דף הרמת הרגל הצידית נפתח אך הניגון לא אומת. גם המקטע המדויק של סיבובי הכתפיים דורש אימות נוסף.

סרטון הנשימה הוא הסבר של איש מקצוע עם תרשימים ומגיש בחלונית; הוא אינו הדגמת גוף מלאה, וכך הוא מתואר בממשק. ההוראות הכתובות והחלופות הקלות זמינות ללא רשת. אין הצגה של אנימציה סכמטית כסרטון אנושי שנבדק. אין אישור גורף שכל 18 ההדגמות מתאימות במדויק או מתנגנות בכל מכשיר.

## החסימה בפרסום והפעולה הנדרשת

ה־workflow `Test and publish FORM 28` נשאר ידני ומפרסם רק `dist`, אחרי כל בדיקות הלוגיקה והדפדפנים. החיבור מאפשר commits, ענפים ו־PR וקריאת ריצות, אך אינו מספק הגדרת Pages, הפעלת workflow ידני או מיזוג רגיל. אין כאן `gh` מאומת. בניסיון להפעיל מיזוג אוטומטי, בקרת האישור האוטומטית דחתה את הפעולה: אישור העלאה ציבורית לא נחשב אצלה אישור מפורש למיזוג לענף הראשי. לא נעשה ניסיון לעקוף את הדחייה. גם הגדרת `allow_auto_merge` במאגר היא false.

להשלמת הפרסום באתר GitHub: למזג את PR #1, לבחור Settings → Pages → Source: GitHub Actions, ואז Actions → Test and publish FORM 28 → Run workflow על main. אין צורך ליצור workflow נוסף. יש להמתין להצלחת הפרסום ולהשתמש בכתובת שהוא מחזיר. לאחר מכן צריך לבדוק את כתובת ה־HTTPS בפועל, קבצי manifest ו־service worker והתקנה במכשיר.

## בדיקות באייפון פיזי שנותרו

Safari והוספה למסך הבית; פתיחה במצב אפליקציה; מגע וגלילה; נעילת מסך ומעבר לאפליקציה אחרת בזמן הטיימר; מצב טיסה לאחר טעינה מקוונת; שמע והשארת מסך דולק; ייצוא ל־Files ושחזור גיבוי; ניגון והתאמה של ההדגמות שעדיין לא אומתו. WebKit בהדמיית iPhone אינו מכשיר פיזי.

לאחר הפרסום: Safari → שיתוף → הוסף למסך הבית → Open as Web App אם מופיע → הוסף. יש לפתוח פעם אחת ברשת ולהמתין לחיווי שהאימונים נשמרו לשימוש ללא רשת. הסרטונים אינם נשמרים במטמון.

דוחות `validation/local-2026-09-16/` הם היסטוריים: ההתקנות בסביבה המקומית נחסמו ב־403. הם אינם מבטלים את תוצאות GitHub Actions המאוחרות יותר.

מקורות לשיטת הבדיקה וההתקנה: [תקלה מדווחת ב־Playwright 1.62](https://github.com/microsoft/playwright/issues/42174), [הגדרת GitHub Pages](https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site), [הוראות Apple](https://support.apple.com/en-gw/guide/iphone/iphea86e5236/ios).
